{{#each cards}}
<div class="col-3">
    <div class="card text-bg-dark pb-1">
        <div class="card-header ">
            {{title}}
            {{#if isTop}}
            <span class="badge text-bg-danger rounded-pill ">Топ</span>
            {{/if}}
            {{#if isNew}}
            <span class="badge text-bg-success rounded-pill ">Нова</span>
            {{/if}}
            {{#if idNew}}
            <span class="badge text-bg-success rounded-pill ">Нова</span>
            {{/if}}
        </div>
        <div class="card-body">
            <p>{{description}}</p>
        </div>
    </div>
</div>
{{/each}}