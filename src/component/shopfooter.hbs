<div class="m-4">
    <div class="col-12 row ">
        {{#each footer}}
        <div class="nav col-2">
            {{#each this}}
            <div class="nav-item">
                <a href="{{link}}" class="nav-link active">{{text}}</a>
            </div>
            {{/each}}
        </div>
        {{/each}}
    </div>
</div>